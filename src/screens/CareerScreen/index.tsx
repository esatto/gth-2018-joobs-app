import React from 'react';
import { View } from 'react-native';

interface CareerProps {}

export class CareerScreen extends React.Component<CareerProps> {
  static navigationOptions = {
    title: 'Karriär',
  };

  render() {
    return <View />;
  }
}
